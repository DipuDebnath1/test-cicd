
name: First Workflow

on: push

jobs:
  my-first-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: Coping code
        uses: actions/checkout@v6

      - name: name node-js
        uses: actions/checkout@v6
        with:
          node-version: 22.7.4
      - name: check node version
        run: node -v